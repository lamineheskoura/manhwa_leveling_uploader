name: ROYAL GHOST ENGINE V26.0

on:
  workflow_dispatch: # للتشغيل اليدوي بضغطة زر سيدي
  schedule:
    - cron: '0 */2 * * *' # تشغيل تلقائي كل ساعتين (اختياري)

jobs:
  execution:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🛡️ Checkout Repository
        uses: actions/checkout@v3

      - name: 🐍 Setup Python Environment
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install Royal Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install DrissionPage telethon requests

      - name: ⚔️ Start Royal Ghost Mission
        env:
          # سحب المفاتيح السرية من إعدادات GitHub سيدي
          NGROK_URL: ${{ secrets.NGROK_URL }}
          SB_URL: ${{ secrets.SB_URL }}
          SB_KEY: ${{ secrets.SB_KEY }}
          # تأكد من إضافة BOT_TOKENS في Secrets إذا أردت تغييرها مستقبلاً
          BOT_TOKENS: ${{ secrets.BOT_TOKENS }} 
        run: python bot_engine.py