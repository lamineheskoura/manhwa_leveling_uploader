name: ROYAL GHOST ENGINE V26.0

on:
  workflow_dispatch: # للتشغيل اليدوي سيدي القائد
  schedule:
    - cron: '0 */2 * * *' # تشغيل تلقائي كل ساعتين لضمان استمرار الغزو

jobs:
  execution:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🛡️ Checkout Repository
        uses: actions/checkout@v3

      - name: 🐍 Setup Python Environment
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install Royal Dependencies
        run: |
          python -m pip install --upgrade pip
          # إضافة المكتبات اللازمة بدقة سيدي
          pip install DrissionPage telethon requests colorama tqdm

      - name: ⚔️ Start Royal Ghost Mission
        env:
          # التأكد من مطابقة الأسماء مع ما في الـ Secrets سيدي
          NGROK_URL: ${{ secrets.NGROK_URL }}
          SB_URL: ${{ secrets.SB_URL }}
          SB_KEY: ${{ secrets.SB_KEY }}
          BOT_TOKENS: ${{ secrets.BOT_TOKENS }} 
        # التعديل الجوهري: استخدام الاسم الذي حددته أنت سيدي
        run: python Cloud_Engine.py