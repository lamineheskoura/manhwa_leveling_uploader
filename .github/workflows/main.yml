name: UC-SOVEREIGN CLOUD ENGINE V5.2

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  conquer:
    runs-on: ubuntu-latest
    timeout-minutes: 25 

    steps:
      - name: 🛡️ Checkout Repository
        uses: actions/checkout@v3

      - name: 🐍 Setup Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 🌐 Install Google Chrome & Correct Dependencies
        run: |
          sudo apt-get update
          # سيدي، قمنا بتحديث أسماء المكتبات لتتوافق مع Ubuntu 24.04 الجديد
          sudo apt-get install -y wget fonts-liberation libappindicator3-1 libasound2t64 libatk-bridge2.0-0 libnspr4 libnss3 libx11-xcb1 libxtst6 xdg-utils libgbm1
          wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo dpkg -i google-chrome-stable_current_amd64.deb
          sudo apt-get install -f -y

      - name: 📦 Install Python Royal Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install undetected-chromedriver==3.5.5 selenium==4.15.2 telethon requests

      - name: ⚔️ Launch UC Sovereign Mission
        env:
          SB_URL: ${{ secrets.SB_URL }}
          SB_KEY: ${{ secrets.SB_KEY }}
          PYTHONUNBUFFERED: 1
        run: python Cloud_Engine.py

      - name: 📋 Report Status
        if: always()
        run: |
          echo "تمت محاولة الغزو سيدي. الحالة: ${{ job.status }}"