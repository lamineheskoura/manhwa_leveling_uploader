# --- Professional Global Commenting Protocol: ROYAL WORKFLOW V8.5 سيدي ---
name: Royal Cloud Engine Deployment

on:
  schedule:
    # سيتم تشغيل المحرك كل 20 دقيقة سيدي لضمان استمرارية الغزو
    - cron: '*/20 * * * *'
  workflow_dispatch: # يتيح لك تشغيل المحرك يدوياً من GitHub سيدي

jobs:
  run-engine:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 سحب الكود من المستودع
        uses: actions/checkout@v3

      - name: 🐍 إعداد بيئة بايثون
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 🌐 تثبيت متصفح Google Chrome
        run: |
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

      - name: 📦 تثبيت المكتبات وتصفية تعارض البروكسي
        run: |
          python -m pip install --upgrade pip
          # تثبيت المكتبات الأساسية لضمان استقرار الاتصال سيدي
          pip install httpx==0.24.1 requests telethon
          # تثبيت باقي المتطلبات من الملف سيدي
          pip install -r requirements.txt

      - name: 🚀 إطلاق المحرك الملكي
        env:
          # ربط الأسرار المخزنة في GitHub ببيئة العمل سيدي
          SB_URL: ${{ secrets.SB_URL }}
          SB_KEY: ${{ secrets.SB_KEY }}
          TG_API_ID: ${{ secrets.TG_API_ID }}
          TG_API_HASH: ${{ secrets.TG_API_HASH }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          BOT_TOKENS: ${{ secrets.BOT_TOKENS }}
          SITE_API_URL: ${{ secrets.SITE_API_URL }}
          SITE_API_KEY: ${{ secrets.SITE_API_KEY }}
        run: python Cloud_Engine.py